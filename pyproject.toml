[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sshmate"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = ["textual>=0.40.0", "pyfiglet"]

[project.scripts]
sshmate = "sshmate.app:main"
